# Speed up Docker builds by excluding files Docker doesn't need.
# (Models are trained inside the image via RUN python train.py)

__pycache__/
*.pyc
*.pyo
.env
*.log
.git
.gitignore

# Exclude any pre-trained local models — they're rebuilt inside Docker
saved_models/
